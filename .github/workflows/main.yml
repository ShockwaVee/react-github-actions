name: "Verify build"
on:
  pull_request:
    branches:
      - master
jobs:
  verifyBuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 # checks out the whole project
      - name: Cache node_modules
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-
      - run: npm ci ## installs dependencies, but faster - similar to yarn install --frozen-lockfile 
      - run: npm run lint:check # make sure you have this script available in your package.json
      - run: npm run test:unit # make sure you have this script available in your package.json

